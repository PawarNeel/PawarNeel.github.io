# Jekyll build output (generated by GitHub Pages — should not be committed)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# macOS
.DS_Store

# Local staging folder for images being imported (not part of the site)
new image imports/
